<h1>Media</h1>
<ul>
  <% venues.forEach(venue => { %>
    <li><a href="/venues/<%= venue._id %>"><%= venue.title %></a></li>
    <li><img src="<%= venue.image %>" alt="<%= venue.title %>'s image"></li>
  <% }); %>
</ul>
